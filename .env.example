# Database Configuration
# Required for production deployment if using PostgreSQL database
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=5000
HOST=0.0.0.0

# Optional: Session Secret (if implementing authentication)
# SESSION_SECRET=your-secret-key-here

# Optional: CORS Origins (if needed for production)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
